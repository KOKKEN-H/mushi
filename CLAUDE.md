# 蟲神器ソロバトル — プロジェクト引き継ぎメモ (CLAUDE.md)

このファイルは Claude Code がこのプロジェクトを開いたときに最初に読むべき文脈です。
`claude.ai` のチャットで Claude と一緒に育ててきたプロトタイプを、ここから
「本格的な Web アプリ」にしていくための引き継ぎ資料です。前回のパッケージから
かなり機能が増えているため、内容を全面的に書き直しています。

## これは何か

蟲神器（ダイソーの昆虫対戦TCG）を1人で遊べるようにした、ブラウザだけで動く
対戦シミュレーターです。現状は **`index.html` 1ファイルにHTML/CSS/JSが
全部入り**（フレームワーク・ビルドステップなし・外部依存は Google Fonts の
CDN読み込みのみ）のプロトタイプで、`file://` で直接開くだけで動作します。

- ブラウザを開いて `index.html` をロードするだけで確認できます。
- npm / ビルドツールは一切使っていません。

## 現状の実装範囲

### カードプール（全158種）
- スターターセット：全24種
- ブースターパック第1弾：全79種（スターターと重複する「コオニヤンマ」は除外）
- ブースターパック第2弾：全55種（虫45・強化5・術5）

コスト・体力・攻撃力・色・レアリティは、公式ファンサイト「蟲神器 最強データベース」
（mushijingi-cardlist.com）のデータを基に正確に再現しています。技の個別効果は、
**共通ルールとして定義されているキーワード技のみ実際の効果を再現**し、それ以外の
カード固有技は名称と数値だけ引き継いで通常攻撃として扱う簡易実装です。

実装済みのキーワード技／効果：
- ＜とびだす＞＜擬態＞＜りんぷん＞＜毒霧噴射＞＜毒の体＞＜紋章＞
- 強化カード：天牛の大顎／蓑虫の隠れ蓑／針金虫の道連れ／玉虫色の羽化／
  無欠の息吹／蚰蜒の足切り／金蚉の甲冑／兜虫の甲冑
- 術カード：虹の架け橋／塵芥虫の爆熱弾／蟲の息吹／飛蝗の凶相／
  電気虫の稲妻／金色の顎門／蟻の収穫／蟲祓いの煙幕

未実装（装着/使用はできるが効果なし）：
- 口寄せの時蛹（強化）・白夜の羽化（術）— 幼虫→成虫の羽化ギミックが複雑なため簡易化

### ルール
- エサ場によるコスト管理、三すくみ属性ダメージ2倍
- 体力は毎ターン終了時に全回復（両プレイヤー分）
- 縄張り（6枚）：虫が破壊された/本体に直接攻撃を受けた時にドロー
- **敗北条件**：縄張りが0の状態で「本体への直接攻撃」を受けた時のみ確定
  （虫が破壊されただけでは、縄張り0でも即敗北にはならず、単にカードが引けないだけ）
- CPUの手札・ドローしたカード名は原則プレイヤーに見えない（非公開情報の秘匿）

### デッキ構築・対戦相手選択
- 自分のデッキは20枚ちょうど・同名最大3枚（プロトタイプ独自の上限）
- カード種別／収録弾（スターター・第1弾・第2弾）／色（赤・青・緑）／
  選択中or未選択、で絞り込めるフィルタ
- 🎲ランダム構築、デッキの保存/読み込み（名前付け対応、JSONファイルの
  ダウンロード/アップロード方式。ブラウザストレージには依存していない）
- 対戦開始前に、CPUのデッキタイプを7種類から選べる
  （ランダム／アグロ／ミッドレンジ／コントロール／赤単／青単／緑単）。
  一般的なTCGのデッキ構築理論（マナカーブ理論）に基づいてコスト分布を
  ターゲット化し、そこから自動構築している。

### CPU AI
- ヒューリスティックベース（探索や評価関数によるルックアヘッドはしていない）
- 強化/術カードも状況に応じて使用（無料カードの活用、除去のタイミング、
  共食い等の特殊技を「通常攻撃で倒せない時だけ」使う判断など）
- 1アクションごとに間隔を空けて進行する非同期ステップ実行（`cpuMainStep`）

### 演出・UI
- 攻撃時：アタッカーが動く、被弾側が震えて星エフェクト
- 破壊時：体力0→攻撃演出→少し間を置いて「瀕死」表示→破裂して消える、の
  2段階演出（`dying`フラグと`DESTROY_DELAY`で管理）
- 直接攻撃で試合が決まる場合も、演出が先に表示されてから勝敗が確定するよう
  タイミング調整済み（過去に演出をすっ飛ばして即ゲームオーバーになるバグを修正した経緯あり）
- ログに出るメッセージは画面中央にも一時的にポップアップ表示
- カード名タップで詳細モーダル（オリジナルSVGアイコン、外部の公式データベースへの
  リンク、自分の実物カードの写真アップロード or 画像URL貼り付け表示に対応。
  実物のカードイラストは著作権上、複製・保存していない）
- 横画面レイアウト対応
- 配色は紫〜ネイビーを基調にした鮮やかなポップテイスト。フォントは
  Google Fonts の "M PLUS Rounded 1c"（要ネット接続。オフライン時は
  フォールバックのシステムフォントになる）

## コード構成（index.html 内）

全てグローバル関数・グローバル変数のシンプルな構成です（モジュール分割なし）。
大きな塊は以下の順に並んでいます。

1. `CARDS` 配列 — 全カードのマスターデータ（スターター→第1弾→第2弾の順）
2. `iconSvg()` / `officialUrl()` — カード情報モーダル用のオリジナルアイコン生成
   と、公式データベースへのリンク組み立て
3. `ARCHETYPES` / `buildArchetypeDeckCounts()` / `buildCpuDeckForArchetype()`
   — CPUデッキのアーキタイプ別自動構築
4. ゲーム状態 (`G`) の構築・フェーズ進行（`startGame` / `toDrawPhase` /
   `placeFeed` / `skipFeed`）
5. カードプレイ・攻撃解決 (`playHandCard` / `executeAttack` /
   `dealCombatOutcome` / `dealDamage` / `destroyBug` / `finalizeDestroy` /
   `territoryDraw`)
   - `dealDamage(...,onDone)` はコールバック形式。致死ダメージの場合は
     `destroyBug` が `DESTROY_DELAY`(520ms) 待ってから `onDone` を呼ぶため、
     カマ連撃や勝敗判定などの後続処理も自動的に破壊演出の完了を待つ。
   - 直接攻撃も同様に、演出→delay→実際の判定、の順で処理する
     （`dealCombatOutcome` の直接攻撃分岐、`resolveCpuNormalAttack` 参照）。
6. CPU AI (`cpuTakeTurn` / `cpuMainStep` / `cpuAttackWith` /
   `resolveCpuNormalAttack` / `playEnhanceCpu` / `resolveSpellCpu`)
7. 選択UI (`askChoice` / `askTargetBug` / `pendingUI` /
   `snapshotState` / `restoreSnapshot` — 「戻る」キャンセル機構)
8. 攻撃演出 (`fx` / `flashAttackFx`) とトースト通知 (`toastMsg` / `showToast`)
9. 描画関数群 (`render` / `renderDeckBuilder` / `renderOpponentSelect` /
   `renderGame` / 各 `*Html()`) と DOM イベントの再バインド (`attach*Handlers`)

`render()` は毎回 `innerHTML` を丸ごと再生成する素朴なフルレンダー方式です。
仮想DOMは使っていません。非同期処理（`setTimeout`）と組み合わせている箇所が
多いため、`G`（ゲーム状態）や `p`/`o`（プレイヤー参照）をクロージャで
キャプチャしたまま`setTimeout`を挟むコードを追加する際は、その間に`G`が
別のゲームに差し替えられていないかの安全ガード
（`if(!G.players.includes(p)) return;` のようなパターン）を入れておくと安全です
（実際に将来のリファクタや自動テストのループで、この手のレースが顕在化した
ことがあります）。

## 画面遷移

`screen` 変数で管理：`'deck'`（デッキ編成）→ `'opponent'`（CPUのデッキタイプ選択）
→ `'game'`（対戦）。対戦終了後は `'deck'` に戻り、`deckCounts`（プレイヤーの
デッキ構成）は明示的にリセットしない限り保持され続ける。

## 既知の制約・今後の課題

- **カードプールが3弾分のみ**：本家は現在7弾・500枚超。第3弾以降を追加する
  場合、同じ公式ファンサイト（`mushijingi-cardlist.com`）のデータを都度参照し、
  著作権のあるカード画像そのものは複製しない方針を継続してください。
- **固有技の簡易実装**：共通キーワード技以外は名称と数値だけ引き継ぎ、通常攻撃
  として扱っている。正確な効果を入れるには当該カードのページを個別に確認して
  実装する必要あり。
- **「口寄せの時蛹」「白夜の羽化」は未実装**：幼虫⇄成虫の羽化ギミックや、
  場に虫を「くっつけて」出す仕組みは、他のカードと設計思想が異なるため
  後回しにしてある。
- **CPU AIは素朴なヒューリスティック**：最適化やmin-max探索などはしていない。
- **単一ファイル構成**：本格的なWebアプリ化にあたっては、以下のような分割・
  刷新を検討する価値があります。
  - カードデータを `data/cards.json` 等に外出しし、`CARDS` はそれを読み込む形に
  - ゲームロジック / 描画 / AI を別ファイル（あるいは軽量フレームワーク導入）に分割
  - ユニットテストの追加（現状はNode上でheadlessに`eval`して手動シミュレーション
    しているだけで、自動テストはゼロ。`tests/headless-sim.js` 参照）
  - デッキ保存をファイルDL/UL方式からアカウント/クラウド同期に発展させるか検討
  - PWA化（オフライン対応・ホーム画面追加、Google Fontsのローカルバンドル化）
  - マルチプレイ対応（現状は1人用。本当の対人戦にするならWebSocket等バックエンド
    が必要）
- **著作権に関する方針**：実際のカードイラストは一切保存・複製しないこと。
  カード情報モーダルのオリジナルSVGアイコン方式と、外部データベースへの
  リンク、ユーザー自身の手持ちカード写真アップロード（メモリ上のみ保持・
  リロードで消える）という現在の方針を維持してください。

## 開発の進め方（提案）

1. まず `index.html` をそのまま `git init` してコミットし、現状を基準点にする。
2. 動作確認用に、ロジック部分をNode上で`eval`してヘッドレスにシミュレーションする
   簡易テストをこのチャットで何度も使っていました。`tests/headless-sim.js` に
   その形をまとめてあるので、`node tests/headless-sim.js [回数]` で実行できます。
   ランダムデッキ同士のCPU対CPU戦を指定回数自動実行し、例外が発生しないか
   確認するだけの雑なスモークテストです。
3. 小さく安全にリファクタリングしながら機能を壊さないよう、リファクタの
   たびに `tests/headless-sim.js` を回してクラッシュがないか確認することを
   推奨します。特に `setTimeout` を挟む非同期処理まわりは壊れやすいので、
   実タイマーのまま（`setTimeout`をモックせず）複数ゲームを回すテストも
   併用すると、タイミング起因のバグ（本チャットで実際に何度か発生した）を
   拾いやすいです。
